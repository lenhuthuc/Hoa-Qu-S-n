spring.application.name=ecommerce
logging.level.org.springframework.security=WARN
# Database Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/hoaquason}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgres}
spring.jpa.hibernate.ddl-auto=update
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=false
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.sql.init.mode=${SPRING_SQL_INIT_MODE:never}
spring.jpa.defer-datasource-initialization=${SPRING_JPA_DEFER_DATASOURCE_INITIALIZATION:false}

# Flyway migration (safe rollout for existing DBs)
spring.flyway.enabled=${SPRING_FLYWAY_ENABLED:false}
spring.flyway.locations=${SPRING_FLYWAY_LOCATIONS:classpath:db/migration}
spring.flyway.baseline-on-migrate=${SPRING_FLYWAY_BASELINE_ON_MIGRATE:true}
spring.flyway.baseline-version=${SPRING_FLYWAY_BASELINE_VERSION:0}
# VnPay config
vnpay.tmnCode=${VNPAY_TMN_CODE:1P6B8S31}
vnpay.hashSecret=${VNPAY_HASH_SECRET:UJNWN4PVHU3DPOZHX3FXZKQVLKEY4W0W}
vnpay.url=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
vnpay.returnUrl=${VNPAY_BACKEND_RETURN_URL:https://api.haquason.uk/api/payments/vnpay/return}
vnpay.frontendReturnUrl=${VNPAY_RETURN_URL:https://haquason.uk/payment/return}
vnpay.ipnUrl=${VNPAY_IPN_URL:https://api.haquason.uk/api/payments/vnpay/ipn}
# MoMo config
momo.partnerCode=${MOMO_PARTNER_CODE:MOMOLRJZ20181206}
momo.accessKey=${MOMO_ACCESS_KEY:mTCp05Wov6S1mUru}
momo.secretKey=${MOMO_SECRET_KEY:uolQWz2A3eG0h1PAnRMTb16cE2lQ6gRk}
momo.endpoint=${MOMO_ENDPOINT:https://test-payment.momo.vn/v2/gateway/api/create}
momo.returnUrl=${MOMO_RETURN_URL:http://localhost:3000/payment/return}
momo.notifyUrl=${MOMO_NOTIFY_URL:http://localhost:8080/api/payments/momo/notify}
#Mail config
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:lechris569@gmail.com}
spring.mail.password=${MAIL_PASSWORD:txcn dtjo pmbo zqhi}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
#Redis config
spring.data.redis.host=${SPRING_DATA_REDIS_HOST:localhost}
spring.data.redis.port=${SPRING_DATA_REDIS_PORT:6379}
spring.data.redis.password=${SPRING_DATA_REDIS_PASSWORD:redis123}
spring.data.redis.repositories.enabled=false

# MongoDB config (Farming Journal)
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/hoaquason}
spring.data.mongodb.auto-index-creation=true

# GHN Shipping API
ghn.token=${GHN_TOKEN:}
ghn.shopId=${GHN_SHOP_ID:}
ghn.baseUrl=${GHN_BASE_URL:https://dev-online-gateway.ghn.vn/shiip/public-api}
ghn.defaultFromDistrictId=${GHN_DEFAULT_FROM_DISTRICT_ID:1542}
ghn.defaultFromWardCode=${GHN_DEFAULT_FROM_WARD_CODE:21012}

# Admin emails (comma-separated)
admin.emails=${ADMIN_EMAILS:admin@hoaquason.com}

# JWT
jwt.secret=${JWT_SECRET:hoaquason_jwt_secret_key_2026_stable!}

# File upload
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=50MB

# App public URLs
app.public-base-url=${APP_PUBLIC_BASE_URL:http://localhost:3000}
app.frontend-base-url=${APP_FRONTEND_BASE_URL:http://localhost:3001}

# Facebook integration
facebook.graph-base-url=${FACEBOOK_GRAPH_BASE_URL:https://graph.facebook.com}
facebook.api-version=${FACEBOOK_API_VERSION:v19.0}
facebook.app-id=${FACEBOOK_APP_ID:}
facebook.app-secret=${FACEBOOK_APP_SECRET:}
facebook.token.encryption-key=${FACEBOOK_TOKEN_ENCRYPTION_KEY:hoaquason_facebook_token_key_32b!}

# OCOP crawler
ocop.crawler.source-url=${OCOP_CRAWLER_SOURCE_URL:https://ocop.gov.vn}
ocop.crawler.format=${OCOP_CRAWLER_FORMAT:html}
ocop.crawler.cron=${OCOP_CRAWLER_CRON:0 0 2 * * *}

# Cloudflare R2 (S3-compatible) Storage
cloud.aws.s3.endpoint=${CLOUDFLARE_R2_ENDPOINT:https://local.r2.cloudflarestorage.com}
cloud.aws.s3.access-key=${S3_ACCESS_KEY:test}
cloud.aws.s3.secret-key=${S3_SECRET_KEY:test}
